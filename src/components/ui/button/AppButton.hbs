<button class="app-button {{class-button}} {{link}}">
    {{#if link}}
        <a href="#" class="app-button-link t5">{{text}}</a>
    {{else}}
        <span class="t3">{{text}}</span>
    {{/if}}
</button>
